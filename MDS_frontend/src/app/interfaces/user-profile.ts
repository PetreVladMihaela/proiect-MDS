export interface UserProfile {
    email: string
    firstName?: string
    lastName?: string
    age?: number
    isMusician?: boolean
    canSing?: boolean
    playedInstrument?: string
    preferredMusicGenre?: string
    bandId?: number
}